# BitLaunch Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Or run the auto-update script after deploying contracts:
#   node scripts/update-env.js

# ─── Network ───
# mainnet | testnet | regtest
VITE_NETWORK=regtest

# ─── Contract Addresses ───
# Set these after deploying contracts (see contracts/README.md)
# Token (OP20):
VITE_CONTRACT_TOKEN=
# Factory (OP20Factory — deploys tokens):
VITE_CONTRACT_FACTORY=
# Presale (template for PresaleFactory cloning):
VITE_CONTRACT_PRESALE=
# Presale Factory (deploys presale instances):
VITE_CONTRACT_PRESALE_FACTORY=
# Vesting:
VITE_CONTRACT_VESTING=
# Liquidity Lock:
VITE_CONTRACT_LOCK=
# Airdrop:
VITE_CONTRACT_AIRDROP=

# ─── Platform Configuration ───
# Wallet address that collects platform fees (lock 0.5%, presale 2%)
VITE_PLATFORM_WALLET=
